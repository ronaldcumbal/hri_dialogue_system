This package was moved out of `ros2_workspace/src` and is no longer part of
the active colcon workspace.

It was built for a Wizard-of-Oz setup around a **Furhat** robot (ZMQ
camera/mic bridges, `furhat_remote_api`) from an earlier project, and has
hardcoded paths tied to that setup's machine. It does not target the
camera-only gaze robot this repository now develops for.

Kept here for reference only. See `DEVELOPMENT_PLAN.md` at the repo root.
